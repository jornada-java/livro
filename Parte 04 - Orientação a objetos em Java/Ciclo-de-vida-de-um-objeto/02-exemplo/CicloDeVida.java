
public class CicloDeVida {
    static int contador;
    // criamos o atributo nome
    String nome;

    public CicloDeVida() {
        // toda vez que o construtor CicloDeVida é invocado é incrementado +1 no contador
        contador++;
    }
}
